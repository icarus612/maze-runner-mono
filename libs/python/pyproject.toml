[project]
name = "maze-runner"
version = "0.1.0"
description = ""
requires-python = ">=3.11"
dependencies = []

[project.scripts]
py-maze-runner = "maze_runner.__main__:main"

[tool.uv]
package = true

[dependency-groups]
dev = ["pytest"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["maze_runner"]
