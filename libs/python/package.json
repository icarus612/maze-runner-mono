{
  "name": "lib.python.maze-runner",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "py-build", 
    "lint": "py-lint",
    "dev": "py-dev",
    "test": "py-test"
  },
  "devDependencies": {
    "lib.bash.build-tools": "workspace:*"
  },
  "bin": {
    "py-maze-runner": "bin/solver.py"
  }
}
